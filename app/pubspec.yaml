name: openroutine
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
# Dependency notes:
# This app pins several deps below their absolute latest release to keep the
# dependency graph resolvable on Flutter 3.41.4 / Dart 3.11.1, which pins
# `meta` to 1.17.0. The latest `drift_dev` / `riverpod_generator` transitively
# require `analyzer` >=10, which requires `meta` ^1.18 — incompatible with
# this Flutter's pinned `meta`. Pinning `drift`/`drift_flutter`/`drift_dev` to
# their 2.3x lines (and `json_annotation`/`freezed_annotation` broad enough to
# match) keeps everyone on `analyzer` <10. Revisit these pins when upgrading
# Flutter — a newer Flutter release will likely bump the `meta` pin and let
# everything move to current majors.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ">=2.6.1 <4.0.0"
  riverpod_annotation: ">=2.6.1 <5.0.0"
  drift: ^2.31.0
  drift_flutter: ^0.2.8
  go_router: ^17.3.0
  freezed_annotation: ">=2.4.0 <4.0.0"
  json_annotation: ">=4.11.0 <5.0.0"
  google_sign_in: ^7.1.0
  flutter_local_notifications: ^22.2.0
  # Required by flutter_local_notifications' zonedSchedule, which takes a
  # TZDateTime. Pure Dart, so it carries no plugin/platform baggage. We only
  # ever schedule offsets from now, so flutter_timezone is not needed — see
  # services/notifications/notification_service.dart.
  timezone: ^0.11.0
  intl: ^0.20.2
  shared_preferences: ^2.5.5
  file_selector: ^1.1.0
  share_plus: ^13.3.0
  json_schema: ^5.2.2
  url_launcher: ^6.3.2
  package_info_plus: ^10.2.1
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Used only by the schema migration test, which builds a real v1 database by
  # hand before opening it with the current code. Already present transitively
  # via drift; declared so the import isn't reaching through another package.
  sqlite3: ^2.9.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  json_serializable: ^6.13.0
  build_runner: ^2.15.1
  drift_dev: ^2.31.0
  riverpod_generator: ^4.0.3
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Generates AppLocalizations from app/lib/l10n/*.arb — see l10n.yaml.
  generate: true

  assets:
    - assets/emojis.json
    - assets/step_templates.json
    - assets/drive_folder_readme.md
    - assets/schemas/routine.schema.json
    - assets/schemas/step.schema.json
    - assets/schemas/trigger.schema.json
    - assets/schemas/completion.schema.json
    - assets/schemas/export.schema.json

  # Bundled rather than fetched via the google_fonts package: that downloads at
  # first use, which would make typography depend on the network in an app
  # whose first principle is local-first and offline (docs/SPEC.md §1).
  # Both families are SIL OFL 1.1 — licence text sits beside the files.
  fonts:
    - family: Lexend
      fonts:
        - asset: assets/fonts/Lexend-400.ttf
          weight: 400
        - asset: assets/fonts/Lexend-500.ttf
          weight: 500
        - asset: assets/fonts/Lexend-600.ttf
          weight: 600
        - asset: assets/fonts/Lexend-700.ttf
          weight: 700
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-400.ttf
          weight: 400
        - asset: assets/fonts/Inter-500.ttf
          weight: 500
        - asset: assets/fonts/Inter-600.ttf
          weight: 600

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
